.container
  = render 'tabs'
  - @articles.each do |article|
    -# = render 'article', article: article 同じフォルダ内にあったのでarticles/articleを省略している
    = render 'commons/article', article: article
    -# commonsfileを作ったことで書き換え
    -# articleが勝手に渡されているわけではない
  = link_to new_article_path do
    .float_btn
      %i.fa.fa-plus
